version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean 
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - $CODEBUILD_SRC_DIR/order-service.jar
