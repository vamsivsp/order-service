version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean 
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/s3://codepipeline-ap-south-1-513521848844/demo-orderservice/SourceArti/
  discard-paths: yes
